<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="form-group">
    <%= f.label :name1 %><br>
    <%= f.text_field :name, :placeholder => "type name here" %> 
  </div>
  <div class="form-group">
    <%= f.label :email %><br>
    <%= f.text_field :email, :placeholder => "type email here" %> 
  </div>

    <div class="form-group">
    <%= f.label :message %><br>
    <%= f.text_area :message, :placeholder => "type message here" %>
  </div>

  <div class="form-group">
    <%= f.submit "Submit", class: "btn brn-lg btn-primary" %>
  </div>
  <br>
<% end %>

