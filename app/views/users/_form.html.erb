<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name, :placeholder => "Type name here." %> 
  </div>
  <div class="form-group">
    <%= f.label :email %><br>
    <%= f.text_field :email, :placeholder => "Type email here." %> 
  </div>

    <div class="form-group">
    <%= f.label :Email_message %><br>
    <%= f.text_area :message, size: "24x6", :placeholder => "Type your message here."  %>
  </div>

  <div class="form-group">
    <%= f.submit "Submit and send E-mail", class: "btn brn-lg btn-primary" %>
  </div>
  <br>
<% end %>

